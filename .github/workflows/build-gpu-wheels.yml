name: build-gpu-wheels

on:
  workflow_dispatch:
    inputs:
      backend:
        description: 'GPU backend to build'
        required: true
        type: choice
        options:
          - cuda-12.4
          - cuda-12.8
          - vulkan
          - all
      publish_index:
        description: 'Publish to GitHub Pages wheel index'
        required: false
        default: false
        type: boolean

env:
  CIBW_BUILD_VERBOSITY: 1
  CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-* cp314-*"
  CIBW_SKIP: "*-musllinux_* *-win32 *-manylinux_i686"

jobs:
  # ---------------------------------------------------------------------------
  # CUDA 12.4 wheels (Linux only)
  # ---------------------------------------------------------------------------
  build_cuda_124:
    name: CUDA 12.4 - Linux x86_64
    if: inputs.backend == 'all' || inputs.backend == 'cuda-12.4'
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install cibuildwheel
        run: pip install cibuildwheel==3.4.0

      - name: Build CUDA wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          CIBW_ARCHS_LINUX: x86_64
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_BEFORE_ALL_LINUX: >
            yum install -y git yum-utils &&
            yum-config-manager --add-repo https://developer.download.nvidia.com/compute/cuda/repos/rhel7/x86_64/cuda-rhel7.repo &&
            yum install -y cuda-nvcc-12-4 cuda-cudart-devel-12-4 libcublas-devel-12-4 &&
            ln -s /usr/local/cuda-12.4 /usr/local/cuda &&
            /opt/python/cp310-cp310/bin/pip install cmake ninja &&
            PATH=/usr/local/cuda/bin:/opt/python/cp310-cp310/bin:$PATH
            GGML_CUDA=1 GGML_METAL=0
            /opt/python/cp310-cp310/bin/python scripts/manage.py build --all --deps-only
          CIBW_ENVIRONMENT_LINUX: >
            GGML_CUDA=1
            GGML_METAL=0
            CUDA_PATH=/usr/local/cuda
            PATH=/usr/local/cuda/bin:$PATH
            LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >
            auditwheel repair -w {dest_dir} {wheel}
            --exclude libcudart.so
            --exclude libcublas.so
            --exclude libcublasLt.so

      - name: List built wheels
        run: ls -lah wheelhouse/

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-cuda-cu124-linux-x86_64
          path: ./wheelhouse/*.whl

  # ---------------------------------------------------------------------------
  # CUDA 12.8 wheels (Linux only)
  # ---------------------------------------------------------------------------
  build_cuda_128:
    name: CUDA 12.8 - Linux x86_64
    if: inputs.backend == 'all' || inputs.backend == 'cuda-12.8'
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install cibuildwheel
        run: pip install cibuildwheel==3.4.0

      - name: Build CUDA wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          CIBW_ARCHS_LINUX: x86_64
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_BEFORE_ALL_LINUX: >
            yum install -y git yum-utils &&
            yum-config-manager --add-repo https://developer.download.nvidia.com/compute/cuda/repos/rhel7/x86_64/cuda-rhel7.repo &&
            yum install -y cuda-nvcc-12-8 cuda-cudart-devel-12-8 libcublas-devel-12-8 &&
            ln -s /usr/local/cuda-12.8 /usr/local/cuda &&
            /opt/python/cp310-cp310/bin/pip install cmake ninja &&
            PATH=/usr/local/cuda/bin:/opt/python/cp310-cp310/bin:$PATH
            GGML_CUDA=1 GGML_METAL=0
            /opt/python/cp310-cp310/bin/python scripts/manage.py build --all --deps-only
          CIBW_ENVIRONMENT_LINUX: >
            GGML_CUDA=1
            GGML_METAL=0
            CUDA_PATH=/usr/local/cuda
            PATH=/usr/local/cuda/bin:$PATH
            LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >
            auditwheel repair -w {dest_dir} {wheel}
            --exclude libcudart.so
            --exclude libcublas.so
            --exclude libcublasLt.so

      - name: List built wheels
        run: ls -lah wheelhouse/

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-cuda-cu128-linux-x86_64
          path: ./wheelhouse/*.whl

  # ---------------------------------------------------------------------------
  # Vulkan wheels (Linux + Windows)
  # ---------------------------------------------------------------------------
  build_vulkan:
    name: Vulkan - ${{ matrix.os }} ${{ matrix.arch }}
    if: inputs.backend == 'all' || inputs.backend == 'vulkan'
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: x86_64
            runner: ubuntu-22.04
            cibw_archs: x86_64
          - os: windows
            arch: x86_64
            runner: windows-2022
            cibw_archs: AMD64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Vulkan SDK (Linux)
        if: matrix.os == 'linux'
        run: |
          wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
          sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-jammy.list https://packages.lunarg.com/vulkan/lunarg-vulkan-jammy.list
          sudo apt update
          sudo apt install -y vulkan-sdk

      - name: Install Vulkan SDK (Windows)
        if: matrix.os == 'windows'
        run: |
          $ver = "1.3.290.0"
          Invoke-WebRequest -Uri "https://sdk.lunarg.com/sdk/download/$ver/windows/VulkanSDK-$ver-Installer.exe" -OutFile VulkanSDK.exe
          Start-Process -FilePath VulkanSDK.exe -ArgumentList "--accept-licenses --default-answer --confirm-command install" -Wait
          echo "VULKAN_SDK=C:\VulkanSDK\$ver" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv (Unix)
        if: matrix.os != 'windows'
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install uv (Windows)
        if: matrix.os == 'windows'
        run: |
          irm https://astral.sh/uv/install.ps1 | iex
          echo "$env:USERPROFILE\.local\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Install dependencies (Unix)
        if: matrix.os != 'windows'
        run: GGML_VULKAN=1 GGML_METAL=0 make sync

      - name: Install dependencies (Windows)
        if: matrix.os == 'windows'
        shell: bash
        run: |
          export GGML_VULKAN=1
          export GGML_METAL=0
          scripts/setup.sh
          uv sync

      - name: Install cibuildwheel
        run: pip install cibuildwheel==3.4.0

      - name: Build Vulkan wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          CIBW_ARCHS: ${{ matrix.cibw_archs }}
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_BEFORE_ALL_LINUX: |
            yum install -y cmake ninja-build
          CIBW_ENVIRONMENT_LINUX: >
            GGML_VULKAN=1
            GGML_METAL=0
          CIBW_ENVIRONMENT_WINDOWS: >
            GGML_VULKAN=1
            GGML_METAL=0
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >
            auditwheel repair -w {dest_dir} {wheel}
            --exclude libvulkan.so
          CIBW_REPAIR_WHEEL_COMMAND_WINDOWS: >
            delvewheel repair -w {dest_dir} {wheel}

      - name: List built wheels
        run: ls -lah wheelhouse/
        shell: bash

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-vulkan-${{ matrix.os }}-${{ matrix.arch }}
          path: ./wheelhouse/*.whl

  # ---------------------------------------------------------------------------
  # Collect and optionally publish to GitHub Pages wheel index
  # ---------------------------------------------------------------------------
  collect:
    name: Collect GPU wheels
    needs: [build_cuda_124, build_cuda_128, build_vulkan]
    if: always() && !cancelled()
    runs-on: ubuntu-latest

    steps:
      - name: Download all wheel artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts
          pattern: wheels-*

      - name: Organize wheels by backend
        run: |
          shopt -s nullglob
          mkdir -p index

          # CUDA 12.4
          wheels=(artifacts/wheels-cuda-cu124-*/*.whl)
          if [ ${#wheels[@]} -gt 0 ]; then
            mkdir -p index/cu124
            cp "${wheels[@]}" index/cu124/
          fi

          # CUDA 12.8
          wheels=(artifacts/wheels-cuda-cu128-*/*.whl)
          if [ ${#wheels[@]} -gt 0 ]; then
            mkdir -p index/cu128
            cp "${wheels[@]}" index/cu128/
          fi

          # Vulkan
          wheels=(artifacts/wheels-vulkan-*/*.whl)
          if [ ${#wheels[@]} -gt 0 ]; then
            mkdir -p index/vulkan
            cp "${wheels[@]}" index/vulkan/
          fi

          echo "=== Wheel index ==="
          find index -name "*.whl" | sort

      - name: Generate PEP 503 index pages
        run: |
          shopt -s nullglob
          for backend_dir in index/*/; do
            [ -d "$backend_dir" ] || continue
            backend=$(basename "$backend_dir")
            mkdir -p "index/${backend}/cyllama"

            # Build file list with hashes
            echo "<!DOCTYPE html><html><body>" > "index/${backend}/cyllama/index.html"
            for whl in "index/${backend}"/*.whl; do
              [ -f "$whl" ] || continue
              filename=$(basename "$whl")
              sha256=$(sha256sum "$whl" | cut -d' ' -f1)
              mv "$whl" "index/${backend}/cyllama/${filename}"
              echo "<a href=\"${filename}#sha256=${sha256}\">${filename}</a><br>" >> "index/${backend}/cyllama/index.html"
            done
            echo "</body></html>" >> "index/${backend}/cyllama/index.html"

            # Root index for this backend
            echo "<!DOCTYPE html><html><body><a href=\"cyllama/\">cyllama</a></body></html>" > "index/${backend}/index.html"
          done

          echo "=== Generated index ==="
          find index -name "*.html" -exec echo {} \; -exec cat {} \; -exec echo \;

      - name: Upload index artifact
        uses: actions/upload-artifact@v4
        with:
          name: gpu-wheel-index
          path: index/

  # ---------------------------------------------------------------------------
  # Publish wheel index to GitHub Pages
  # ---------------------------------------------------------------------------
  publish_index:
    name: Publish to GitHub Pages
    needs: collect
    if: inputs.publish_index
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages

    steps:
      - name: Download index
        uses: actions/download-artifact@v5
        with:
          name: gpu-wheel-index
          path: wheel-index

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: wheel-index

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
